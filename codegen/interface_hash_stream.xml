<interface name = "hash stream">
    Provide interface to calculate hash (message digest) over a stream.

    <inherit interface = "hash" />

    <method name = "start">
        Start a new hashing.
    </method>

    <method name = "update">
        Add given data to the hash.

        <argument name = "data" type = "data"/>
    </method>

    <method name = "finish">
        Accompilsh hashing and return the it's result (a message digest).

        <argument name = "digest" type = "data" size = "known" io = "out">
            <size source = "hash info" constant = "digest size" />
        </argument>
    </method>
</interface>
