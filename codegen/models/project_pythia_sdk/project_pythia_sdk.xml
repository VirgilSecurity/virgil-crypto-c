<project
        name="pythia sdk"
        brief="Virgil Security Pythia SDK"
        package="sdk pythia"
        namespace="virgil sdk pythia"
        framework="VSSPyhia"
        prefix="vssp"
        wrappers=""
        >

    <version major="0" minor="16" patch="0"/>

    Virgil Security provides an SDK which allows you to communicate with Virgil Pythia Service
    and implement Pythia protocol in order to generate user's BrainKey.
    BrainKey is a user's Private Key which is based on user's password.
    BrainKey can be easily restored and is resistant to online and offline attacks.

    <require project="common" feature="library"/>
    <require project="foundation" feature="library"/>
    <require project="pythia" feature="library"/>
    <require project="core sdk" feature="library"/>


    <feature name="multi threading" default="on">
        Enable multi-threading safety for Pythia SDK.
    </feature>

    <module name="assert" from="shared"/>
    <module name="library" from="shared"/>
    <module name="memory" from="shared"/>
    <module name="atomic" from="shared"/>

    <enum name="status"/>

    <!-- Common modules -->
    <class name="error" from="shared"/>

    <!-- Pythia modules -->
    <class name="pythia client"/>

    <!-- BrainKey modules -->
    <class name="brain key seed"/>

    <error_message_getter success="0">
        <require enum="status"/>

        char error_message[48] = {0x00};
        snprintf(error_message, sizeof(error_message), "Unhandled vsc::pythia_sdk error -0x%04x", error);
    </error_message_getter>
</project>
