<class name="signer">
    Sign data of any size.

    <require impl="sha384"/>
    <require interface="alg"/>
    <require interface="sign hash"/>

    <dependency name="hash" interface="hash"/>

    <property name="asn1wr" impl="asn1wr"/>
    <property name="alg info der serializer" impl="alg info der serializer"/>

    <method name="reset">
        Start a processing a new signature.
    </method>

    <method name="update">
        Add given data to the signed data.

        <argument name="data" class="data"/>
    </method>

    <method name="signature len">
        Return length of the signature.

        <argument name="private key" interface="sign hash"/>

        <return type="size"/>
    </method>

    <method name="sign">
        Accomplish signing and return signature.

        <argument name="private key" interface="sign hash" access="readwrite"/>
        <argument name="signature" class="buffer">
            <length method="signature len">
                <proxy argument="private" to="private key"/>
            </length>
        </argument>

        <return enum="status"/>
    </method>
</class>
