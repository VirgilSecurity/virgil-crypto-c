<interface name="generate ephemeral key">
    Provide interface to compute shared key for 2 asymmetric keys.

    Assume that this interface is implemented on the private key.

    <method name="generate ephemeral key">
        Generate ephemeral private key of the same type.

        <argument name="error" class="error" access="readwrite"/>

        <return interface="private key" access="disown"/>
    </method>
</interface>
