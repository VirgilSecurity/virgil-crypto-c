<enum name="error" definition="public">
    Defines library error codes.

    <constant name="success" of_class="global" value="0">
        No errors was occurred.
    </constant>

    <constant name="bad arguments" value="-1">
        This error should not be returned if assertions is enabled.
    </constant>

    <constant name="uninitialized" value="-2">
        Can be used to define that not all context prerequisites are satisfied.
        Note, this error should not be returned if assertions is enabled.
    </constant>

    <constant name="unhandled thirdparty error" value="-3">
        Define that error code from one of third-party module was not handled.
        Note, this error should not be returned if assertions is enabled.
    </constant>

    <constant name="small buffer" value="-101">
        Buffer capacity is not enaugh to hold result.
    </constant>

    <constant name="auth failed" value="-201">
        Authentication failed during decryption.
    </constant>

    <constant name="out of data" value="-202">
        Attempt to read data out of buffer bounds.
    </constant>

    <constant name="bad asn1" value="-203">
        ASN.1 encoded data is corrupted.
    </constant>

    <constant name="asn1 lossy type narrowing" value="-204">
        Attempt to read ASN.1 type that is bigger then requested C type.
    </constant>

    <constant name="bad pkcs1 public key" value="-205">
        ASN.1 representation of PKCS#1 public key is corrupted.
    </constant>

    <constant name="bad pkcs1 private key" value="-206">
        ASN.1 representation of PKCS#1 private key is corrupted.
    </constant>

    <constant name="bad pkcs8 public key" value="-207">
        ASN.1 representation of PKCS#8 public key is corrupted.
    </constant>

    <constant name="bad pkcs8 private key" value="-208">
        ASN.1 representation of PKCS#8 private key is corrupted.
    </constant>

    <constant name="bad encrypted data" value="-209">
        Encrypted data is corrupted.
    </constant>

    <constant name="random failed" value="-210">
        Underlying random operation returns error.
    </constant>

    <constant name="key generation failed" value="-211">
        Generation of the private or secret key failed.
    </constant>

    <constant name="entropy source failed" value="-212">
        One of the entropy sources failed.
    </constant>

    <constant name="rng requested data too big" value="-213">
        Requested data to be generated is too big.
    </constant>

    <constant name="bad base64" value="-214">
        Base64 encoded string contains invalid characters.
    </constant>
</enum>
