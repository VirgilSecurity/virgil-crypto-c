<library
        name="nanopb"
        path="../thirdparty/nanopb/"
        prefix="pb">

    External library: https://github.com/nanopb/nanopb


    <feature name="ENABLE_MALLOC" default="off">
        Enable support for dynamically allocated fields
    </feature>

    <feature name="NO_PACKED_STRUCTS" default="off">
        Define this if your CPU / compiler combination does not support unaligned memory access to packed structures.
    </feature>

    <feature name="MAX_REQUIRED_FIELDS" default="off">
        Increase the number of required fields that are tracked. A compiler warning will tell if you need this.
    </feature>

    <feature name="FIELD_16BIT" default="off">
        Add support for tag numbers > 255 and fields larger than 255 bytes.
    </feature>

    <feature name="FIELD_32BIT" default="on">
        Add support for tag numbers > 65536 and fields larger than 65536 bytes.
    </feature>

    <feature name="NO_ERRMSG" default="off">
        Disable support for error messages in order to save some code space.
    </feature>

    <feature name="BUFFER_ONLY" default="off">
        Disable support for custom streams (support only memory buffers).
    </feature>

    <feature name="OLD_CALLBACK_STYLE" default="off">
        Switch back to the old-style callback function signature. This was the default until nanopb-0.2.1.
    </feature>
</library>
