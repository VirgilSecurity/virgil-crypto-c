# Task: CG-057 - Generate Umbrella and Support Headers

**Created:** 2026-04-11
**Size:** M

## Review Level: 1 (Plan Only)

**Assessment:** Generate 10 files: 2 umbrella headers (`foundation_public.h`, `foundation_private.h`), 1 private implementation header (`hkdf_private.h`), 1 utility header (`endianness.h`), 1 constant header (`pem_title.h`), and 5 mbedTLS bridge headers. Mix of generated and handwritten content — need to determine which are fully generated vs partially generated vs purely handwritten.
**Score:** 2/8 — Blast radius: 1, Pattern novelty: 1, Security: 0, Reversibility: 0

## Canonical Task Folder

```
taskplane-tasks/codegen/CG-057-generate-umbrella-and-support-headers/
├── PROMPT.md   ← This file (immutable above --- divider)
├── STATUS.md   ← Execution state (worker updates this)
├── .reviews/   ← Reviewer output (created by the orchestrator runtime)
└── .DONE       ← Created when complete
```

## Mission

Generate the 10 private helper / umbrella / support headers that exist in legacy but not in new codegen output:

**Umbrella headers (auto-generated from module list):**
1. `vscf_foundation_public.h` — public umbrella, `#include`s all public module headers
2. `vscf_foundation_private.h` — private umbrella, `#include`s all private headers

**Private/support headers:**
3. `vscf_hkdf_private.h` — private methods for HKDF (extract/expand)
4. `vscf_endianness.h` — byte-order utility macros
5. `vscf_pem_title.h` — PEM title string constants

**mbedTLS bridge headers (handwritten):**
6. `vscf_mbedtls_bignum_asn1_reader.h`
7. `vscf_mbedtls_bignum_asn1_writer.h`
8. `vscf_mbedtls_bridge_entropy_poll.h`
9. `vscf_mbedtls_ecp.h`
10. `vscf_mbedtls_md.h`

**Important:** Some of these may be purely handwritten (not generated by legacy codegen). The task should first audit each file to determine if it's generated, partially generated, or handwritten. Only generate files that the legacy codegen produces — handwritten files should be noted as out-of-scope discoveries.

## Dependencies

None.

## Context to Read First

**Tier 2 (area context):**
- `taskplane-tasks/codegen/CONTEXT.md`

**Specific files to examine:**
- Each of the 10 legacy files listed above
- Check for `@generated` / `@<tag>` markers indicating generated content
- The `common` project umbrella headers for pattern reference:
  - `library/common/include/virgil/crypto/common/vsc_common_public.h`
  - `library/common/include/virgil/crypto/common/private/vsc_common_private.h`

## Environment

- **Workspace:** `tools/codegen/`
- **Services required:** cmake build system configured

## File Scope

- `tools/codegen/project_c_backend.py` (umbrella header renderer)
- `tools/codegen/common_bootstrap.py` (if umbrella generation exists for common)

## Steps

### Step 0: Preflight

- [ ] Audit each of the 10 files: check for `@generated` tags, determine generated vs handwritten
- [ ] Study the `common` project umbrella headers — are they generated by the new codegen already?
- [ ] Categorize: which files should the codegen generate, which are handwritten (out of scope)
- [ ] For generated files, extract the generation pattern (include list, guards, ordering)

### Step 1: Generate umbrella headers

> ⚠️ Hydrate: Expand based on audit results from Step 0

- [ ] Implement umbrella header generation (public + private) in `project_c_backend.py`
- [ ] Generate the include list from the set of rendered modules
- [ ] Handle correct ordering and include paths
- [ ] Wire into auto-discovery / output registration

### Step 2: Generate other support headers (if generated)

- [ ] Generate `vscf_hkdf_private.h` if it's codegen-produced (private method declarations)
- [ ] Generate other headers identified as codegen-produced in Step 0
- [ ] Document handwritten files as out-of-scope

### Step 3: Verification

> ZERO test failures allowed.

- [ ] Run FULL Python test suite: `PYTHONPATH=. python3 -m unittest discover -s tools/codegen -p "test_*.py"`
- [ ] Run common build gate: `bash tools/codegen/build_common_with_new_codegen.sh`
- [ ] Run foundation generation: `bash tools/codegen/new_codegen.sh foundation`
- [ ] Verify umbrella headers are generated with correct include lists
- [ ] Run foundation build: `bash tools/codegen/new_codegen.sh --verify foundation`
- [ ] Fix any regressions

### Step 4: Documentation & Delivery

- [ ] Update `taskplane-tasks/codegen/CONTEXT.md` — update file generation counts, note handwritten files
- [ ] Discoveries logged in STATUS.md

## Documentation Requirements

**Must Update:**
- `taskplane-tasks/codegen/CONTEXT.md` — update legacy-only file counts, categorize handwritten files

## Completion Criteria

- [ ] All steps complete
- [ ] All Python tests passing
- [ ] Common build gate passes
- [ ] All codegen-produced support headers generated
- [ ] Handwritten files documented as out-of-scope
- [ ] Documentation updated

## Git Commit Convention

- **Step completion:** `feat(CG-057): complete Step N — description`
- **Bug fixes:** `fix(CG-057): description`
- **Hydration:** `hydrate: CG-057 expand Step N checkboxes`

## Do NOT

- Modify handwritten `.c` files in `library/foundation/src/`
- Commit generated `library/common/**` or `library/foundation/**` outputs
- Try to generate purely handwritten files — document them and move on
- Commit without the task ID prefix

---

## Amendments (Added During Execution)
