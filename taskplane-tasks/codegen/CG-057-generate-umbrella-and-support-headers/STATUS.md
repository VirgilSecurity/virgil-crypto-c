# CG-057: Generate Umbrella and Support Headers — Status

**Current Step:** Step 2: Generate other support headers
**Status:** 🟡 In Progress
**Last Updated:** 2026-04-11
**Review Level:** 1
**Review Counter:** 0
**Iteration:** 1
**Size:** M

---

### Step 0: Preflight
**Status:** ✅ Complete

- [x] Audit each of the 10 files: check for `@generated` tags, determine generated vs handwritten
- [x] Study the `common` project umbrella headers — are they generated by the new codegen already?
- [x] Categorize: which files should the codegen generate, which are handwritten (out of scope)
- [x] For generated files, extract the generation pattern (include list, guards, ordering)

---

### Step 1: Generate umbrella headers
**Status:** ✅ Complete

- [x] Add `render_umbrella_public_header()` and `render_umbrella_private_header()` to `project_c_backend.py` — collect public/private headers from IR entities, produce full file content matching legacy structure
- [x] Add umbrella header writing to `common_bootstrap.py` main loop — generate for both common and foundation projects, write full files (not merge)
- [x] Verify common umbrella headers match existing files
- [x] Verify foundation umbrella headers match existing files (public matches exactly; private is superset of legacy — includes _internal.h files from CG-054)

---

### Step 2: Generate other support headers (if generated)
**Status:** ✅ Complete

- [x] Verify all 8 non-umbrella support headers are already generated by existing codegen (from class/module IR)
- [x] Document findings: 6/8 now generated (mbedtls_bignum x2, bridge_entropy_poll, ecp, md, pem_title) after IR fix for scope=internal; 2/8 not generated (endianness=dead code, hkdf_private=methods now in main header)
- [x] `vscf_hkdf_private.h` private methods (extract/expand) already in vscf_hkdf.h with VSCF_PRIVATE — separate _private.h is legacy artifact, not needed by new codegen

---

### Step 3: Verification
**Status:** ⬜ Not Started

- [ ] Run FULL Python test suite: `PYTHONPATH=. python3 -m unittest discover -s tools/codegen -p "test_*.py"`
- [ ] Run common build gate: `bash tools/codegen/build_common_with_new_codegen.sh`
- [ ] Run foundation generation: `bash tools/codegen/new_codegen.sh foundation`
- [ ] Verify umbrella headers are generated with correct include lists
- [ ] Run foundation build: `bash tools/codegen/new_codegen.sh --verify foundation`
- [ ] Fix any regressions

---

### Step 4: Documentation & Delivery
**Status:** ⬜ Not Started

- [ ] Update `taskplane-tasks/codegen/CONTEXT.md` — update file generation counts, note handwritten files
- [ ] Discoveries logged in STATUS.md
- [ ] All steps complete
- [ ] All Python tests passing
- [ ] Common build gate passes
- [ ] All codegen-produced support headers generated
- [ ] Handwritten files documented as out-of-scope
- [ ] Documentation updated

---

## Reviews

| # | Type | Step | Verdict | File |
|---|------|------|---------|------|

---

## Discoveries

| Discovery | Disposition | Location |
|-----------|-------------|----------|
| All 10 files have `@generated` markers — all are partially generated by legacy codegen | Scope: all should be generated | Various |
| `vscf_hkdf_private.h` is at `private/` include dir (not `src/`) | Path correction | `library/foundation/include/.../private/vscf_hkdf_private.h` |
| `vscf_endianness.h` is at `private/` include dir (not root include) | Path correction | `library/foundation/include/.../private/vscf_endianness.h` |
| `vscf_pem_title.h` is in `src/` dir (internal header) | Confirmed | `library/foundation/src/vscf_pem_title.h` |
| Umbrella headers have includes in non-generated section; `@generated` is empty | Need full-file generation, not merge | Both umbrella headers |
| 5 mbedTLS bridge headers + pem_title + endianness + hkdf_private all have XML models OR are generated from implementation models | Legacy codegen source | `codegen/models/project_foundation/` |
| `vscf_endianness.h` has NO XML model — declares `reverse_memcpy` with no implementation (dead code) | Handwritten dead code, out of scope | `library/foundation/include/.../private/vscf_endianness.h` |
| Common umbrella headers are NOT generated by new codegen | Need to add for both common and foundation | `common_bootstrap.py` |
| `vscf_hkdf_private.h` has no XML model — private methods now in main `vscf_hkdf.h` by new codegen | Legacy artifact, not needed | `vscf_hkdf.h` |
| IR `build_output_target` didn't handle `scope=internal` — fixed: internal headers now point to `src/` | Fixed in `project_ir.py` | `build_output_target()` |
| 6 support headers (.h) now generated after IR fix (were only generating .c before) | Bug fix | `project_ir.py` |

---

## Execution Log

| Timestamp | Action | Outcome |
|-----------|--------|---------|
| 2026-04-11 | Task staged | STATUS.md auto-generated by task-runner |
| 2026-04-11 17:26 | Task started | Runtime V2 lane-runner execution |
| 2026-04-11 17:26 | Step 0 started | Preflight |

---

## Blockers

*None*

---

## Notes

*Reserved for execution notes*