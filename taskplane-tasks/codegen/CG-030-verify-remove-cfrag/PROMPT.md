# Task: CG-030 - Verify Lifecycle Generation and Remove cfrag Files

**Created:** 2026-04-08
**Size:** S

## Review Level: 1 (Plan Only)

**Assessment:** Cleanup/removal task. The actual code generation was done in CG-028/CG-029. This task verifies parity and removes the now-redundant cfrag files and extra_methods override.
**Score:** 2/8 — Blast radius: 1, Pattern novelty: 0, Security: 0, Reversibility: 1

## Canonical Task Folder

```
taskplane-tasks/codegen/CG-030-verify-remove-cfrag/
├── PROMPT.md   ← This file (immutable above --- divider)
├── STATUS.md   ← Execution state (worker updates this)
├── .reviews/   ← Reviewer output (created by the orchestrator runtime)
└── .DONE       ← Created when complete
```

## Mission

Verify that the lifecycle method bodies generated by the shared C backend (CG-028) produce identical output to the current `.cfrag` files for `vsc_buffer`, then remove the cfrag files, the `extra_methods` override in `common_bootstrap.py`, and the `ClassMethodSpec` import/usage. This completes the ADR 0004 follow-up: `.cfrag` files are replaced by generic lifecycle generation rules.

## Dependencies

- **Task:** CG-029 (dependency management methods must be in place so the full class rendering is complete)

## Context to Read First

**Tier 2 (area context):**
- `taskplane-tasks/codegen/CONTEXT.md`

**Tier 3 (load only if needed):**
- `docs/adr/0004-universal-model-driven-codegen.md` — the ADR this task completes

## Environment

- **Workspace:** `tools/codegen/`
- **Services required:** None

## File Scope

- `tools/codegen/common_bootstrap.py`
- `tools/codegen/project_c_backend.py`
- `tools/codegen/support/common_runtime/buffer/*.cfrag` (deleted)

## Steps

### Step 0: Preflight

- [ ] CG-028 and CG-029 are complete
- [ ] Build gate passes with current state: `bash tools/codegen/build_common_with_new_codegen.sh`
- [ ] All cfrag files still exist at `tools/codegen/support/common_runtime/buffer/`

### Step 1: Verify parity between generated and cfrag bodies

Compare the lifecycle method bodies that the C backend now generates for `vsc_buffer` against each cfrag file's content. They must be semantically equivalent (identical C logic, whitespace differences are acceptable).

Files to verify against:
- `init.cfrag` → generated init body
- `cleanup.cfrag` → generated cleanup body
- `new.cfrag` → generated new body
- `delete.cfrag` → generated delete body
- `destroy.cfrag` → generated destroy body
- `shallow_copy.cfrag` → generated shallow_copy body
- `init_with_capacity.cfrag` → generated init_with_capacity body
- `new_with_capacity.cfrag` → generated new_with_capacity body
- `init_with_data.cfrag` → generated init_with_data body
- `new_with_data.cfrag` → generated new_with_data body

- [ ] Write a verification script or test that extracts generated bodies and compares to cfrag content
- [ ] All 10 lifecycle method bodies match
- [ ] Run targeted test to confirm

**Artifacts:**
- Verification script or test (new)

### Step 2: Remove cfrag files and extra_methods override

- [ ] Remove the `extra_methods` tuple and `ClassMethodSpec` usage from `build_direct_buffer_c_module()` in `common_bootstrap.py`
- [ ] Remove the `_SUPPORT_DIR` constant if no longer used
- [ ] Remove the `ClassMethodSpec` import from `common_bootstrap.py` if no longer used anywhere
- [ ] Delete all `.cfrag` files: `tools/codegen/support/common_runtime/buffer/*.cfrag`
- [ ] Delete the empty directory tree if no other files remain: `tools/codegen/support/`
- [ ] Consider whether `ClassMethodSpec` and `load_support_code` in `project_c_backend.py` should be removed (if no other callers)

**Artifacts:**
- `tools/codegen/common_bootstrap.py` (modified)
- `tools/codegen/project_c_backend.py` (modified, if ClassMethodSpec removed)
- `tools/codegen/support/common_runtime/buffer/*.cfrag` (deleted)

### Step 3: Testing & Verification

> ZERO test failures allowed.

- [ ] Run FULL test suite: `python3 -m py_compile tools/codegen/project_source.py tools/codegen/project_ir.py tools/codegen/project_c_backend.py tools/codegen/common_bootstrap.py`
- [ ] Build passes: `bash tools/codegen/build_common_with_new_codegen.sh`
- [ ] Verify buffer C output is byte-identical to pre-removal output
- [ ] Fix all failures

### Step 4: Documentation & Delivery

- [ ] Update ADR 0004 to mark the cfrag follow-up as completed
- [ ] Update CONTEXT.md: note cfrag files removed, lifecycle generation is fully rule-based
- [ ] Discoveries logged in STATUS.md

## Documentation Requirements

**Must Update:**
- `docs/adr/0004-universal-model-driven-codegen.md` — mark follow-up section as completed
- `taskplane-tasks/codegen/CONTEXT.md` — update current state

**Check If Affected:**
- `docs/codegen-migration/README.md` — update if it references cfrag files

## Completion Criteria

- [ ] All cfrag files deleted
- [ ] extra_methods override removed from common_bootstrap.py
- [ ] Buffer C output unchanged (byte-identical before and after)
- [ ] Build gate passes
- [ ] ADR 0004 updated
- [ ] All tests passing

## Git Commit Convention

- **Step completion:** `feat(CG-030): complete Step N — description`
- **Tests:** `test(CG-030): description`
- **Hydration:** `hydrate: CG-030 expand Step N checkboxes`

## Do NOT

- Expand task scope — add tech debt to CONTEXT.md instead
- Skip tests
- Modify framework/standards docs without explicit user approval (exception: ADR 0004 update is explicitly required)
- Load docs not listed in "Context to Read First"
- Commit without the task ID prefix in the commit message
- Modify lifecycle generation logic — if it doesn't match cfrag, fix in CG-028 not here

---

## Amendments (Added During Execution)

<!-- Workers add amendments here if issues discovered during execution. -->
