name: build-python-wheels
on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  build-wheels:
    name: Build wheels – ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: linux-x86_64
            os: ubuntu-latest
            cibw_archs: x86_64
            extra_cmake_args: "-DED25519_AMD64_RADIX_64_24K=ON -DED25519_REF10=OFF"
          - name: linux-aarch64
            os: ubuntu-24.04-arm
            cibw_archs: aarch64
            extra_cmake_args: ""
          - name: macos-universal
            os: macos-14
            cibw_archs: "x86_64 arm64"
            extra_cmake_args: ""
          - name: windows-x86_64
            os: windows-latest
            cibw_archs: AMD64
            extra_cmake_args: "-DVIRGIL_LIB_PYTHIA=OFF"

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install cibuildwheel
        run: python -m pip install cibuildwheel==2.21.3

      - name: Build wheels
        run: python -m cibuildwheel --output-dir wheelhouse wrappers/python
        env:
          CIBW_ARCHS: ${{ matrix.cibw_archs }}
          CIBW_SKIP: "pp* *-musllinux_*"

          # -- Linux ----------------------------------------------------------
          CIBW_ENVIRONMENT_LINUX: >-
            LD_LIBRARY_PATH={project}/wrappers/python/virgil_crypto_lib/_libs
          CIBW_BEFORE_ALL_LINUX: >-
            yum install -y cmake3 gcc-c++ &&
            ln -sf /usr/bin/cmake3 /usr/local/bin/cmake &&
            cd {project} &&
            cmake -Cconfigs/python-config.cmake
            -DCMAKE_BUILD_TYPE=Release
            -DCMAKE_INSTALL_PREFIX=wrappers/python/virgil_crypto_lib
            -DCMAKE_INSTALL_LIBDIR=_libs
            -DENABLE_CLANGFORMAT=OFF
            ${{ matrix.extra_cmake_args }}
            -Bbuild -H.
            && cmake --build build --target install -- -j$(nproc)

          # -- macOS -----------------------------------------------------------
          CIBW_BEFORE_ALL_MACOS: >-
            cd {project} &&
            cmake -Cconfigs/python-config.cmake
            -DCMAKE_BUILD_TYPE=Release
            -DCMAKE_OSX_ARCHITECTURES="arm64;x86_64"
            -DCMAKE_INSTALL_PREFIX=wrappers/python/virgil_crypto_lib
            -DCMAKE_INSTALL_LIBDIR=_libs
            -DENABLE_CLANGFORMAT=OFF
            -Bbuild -H.
            && cmake --build build --target install -- -j$(sysctl -n hw.physicalcpu)

          # -- Windows ---------------------------------------------------------
          CIBW_BEFORE_ALL_WINDOWS: >-
            cd /d {project} &&
            cmake -Cconfigs/python-config.cmake
            ${{ matrix.extra_cmake_args }}
            -DCMAKE_BUILD_TYPE=Release
            -DCMAKE_INSTALL_PREFIX=wrappers/python/virgil_crypto_lib
            -DCMAKE_INSTALL_LIBDIR=_libs
            -DCMAKE_INSTALL_BINDIR=_libs
            -DENABLE_CLANGFORMAT=OFF
            -Bbuild -H.
            && cmake --build build --target install --config Release

      - uses: actions/upload-artifact@v4
        with:
          name: wheels-${{ matrix.name }}
          path: ./wheelhouse/*.whl

  build-sdist:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Build sdist
        run: python -m pip install build && python -m build --sdist wrappers/python --outdir dist

      - uses: actions/upload-artifact@v4
        with:
          name: sdist
          path: dist/*.tar.gz
